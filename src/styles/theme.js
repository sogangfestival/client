const onModal = {
  overflow: "hidden",
  bgColor: "rgba(255,255,255,0.7)",
};

const offModal = {
  overflow: "visible",
  bgColor: "white",
};

const theme = {
  onModal,
  offModal,
};

export default theme;
